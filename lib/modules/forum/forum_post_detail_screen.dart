// ============================================================
// forum_post_detail_screen.dart
// Pantalla de detalle de una publicación del foro
// TODO: implementar en el módulo correspondiente.
// ============================================================

import 'package:flutter/material.dart';

/// Pantalla de detalle de una publicación del foro
class ForumPostDetailScreen extends StatelessWidget {
  const ForumPostDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ForumPostDetailScreen — próximamente'),
      ),
    );
  }
}
