// ============================================================
// os10_results_screen.dart
// Pantalla de resultados del simulacro OS10
// TODO: implementar en el módulo correspondiente.
// ============================================================

import 'package:flutter/material.dart';

/// Pantalla de resultados del simulacro OS10
class Os10ResultsScreen extends StatelessWidget {
  const Os10ResultsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Os10ResultsScreen — próximamente'),
      ),
    );
  }
}
