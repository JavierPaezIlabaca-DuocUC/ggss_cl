// ============================================================
// os10_question_screen.dart
// Pantalla de pregunta del simulador OS10
// TODO: implementar en el módulo correspondiente.
// ============================================================

import 'package:flutter/material.dart';

/// Pantalla de pregunta del simulador OS10
class Os10QuestionScreen extends StatelessWidget {
  const Os10QuestionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Os10QuestionScreen — próximamente'),
      ),
    );
  }
}
