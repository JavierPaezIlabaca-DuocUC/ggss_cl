// ============================================================
// academic_detail_screen.dart
// Pantalla de detalle de una oferta académica
// TODO: implementar en el módulo correspondiente.
// ============================================================

import 'package:flutter/material.dart';

/// Pantalla de detalle de una oferta académica
class AcademicDetailScreen extends StatelessWidget {
  const AcademicDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('AcademicDetailScreen — próximamente'),
      ),
    );
  }
}
